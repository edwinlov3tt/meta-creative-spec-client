# ============================================
# Meta Creative Spec - Client Environment Variables
# ============================================
# This file documents all environment variables used by the React frontend.
# Copy this file to .env.local for local development.

# ============================================
# API Configuration
# ============================================

# API Base URL
# - PRODUCTION (Vercel): Leave EMPTY or REMOVE this variable entirely
#   Vercel will proxy all /api/* requests to Railway backend automatically
# - DEVELOPMENT: Use http://localhost:3001 (or your local backend port)
VITE_API_URL=http://localhost:3001

# Socket.IO Server URL
# - PRODUCTION (Vercel): Leave EMPTY or REMOVE this variable entirely
#   Vercel will proxy all /socket.io/* requests to Railway backend automatically
# - DEVELOPMENT: Use http://localhost:3001 (or your local backend port)
VITE_SOCKET_URL=http://localhost:3001

# ============================================
# Deployment Instructions
# ============================================

# For Vercel Production Deployment:
# 1. Go to: https://vercel.com/[your-project]/settings/environment-variables
# 2. Add variables with EMPTY values (or don't add them at all):
#    - VITE_API_URL = "" (empty)
#    - VITE_SOCKET_URL = "" (empty)
# 3. Select "Production" environment
# 4. The code defaults to empty strings in production, which uses relative URLs

# For Local Development:
# 1. Copy this file to .env.local
# 2. Update VITE_API_URL and VITE_SOCKET_URL to point to your local backend
# 3. Make sure your local backend server is running on the specified port
# 4. Run: npm run dev

# ============================================
# How It Works
# ============================================

# Production Flow (Vercel → Railway):
#   Frontend: https://meta-creative-spec-client.vercel.app
#   API Request: fetch('/api/ads')
#   Vercel Proxy: Forwards to https://meta-creative-spec-production.up.railway.app/api/ads
#   Result: No CORS errors, all requests appear same-origin
#
# Development Flow (localhost:5173 → localhost:3001):
#   Frontend: http://localhost:5173
#   API Request: fetch('http://localhost:3001/api/ads')
#   Direct Connection: Frontend connects directly to local backend
#   Result: Normal CORS handling for local development
